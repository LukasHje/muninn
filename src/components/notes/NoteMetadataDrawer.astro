---
import Icon from "src/components/Icon.astro";
import NoteProperties from "src/components/notes/NoteProperties.astro";
import { ui } from "src/i18n";
import type { LibraryItem } from "src/lib/vault";

interface Props {
	note: LibraryItem;
}

const { note } = Astro.props;
---

<button
	type="button"
	class="note-metadata-trigger"
	data-note-metadata-open
	aria-label={ui.noteDetail.openMetadata}
	aria-controls="note-metadata-drawer"
	aria-expanded="false"
>
	<Icon name="sliders-horizontal" class="h-5 w-5" />
	<span class="sr-only">{ui.noteDetail.openMetadata}</span>
</button>

<aside
	id="note-metadata-drawer"
	class="note-metadata-drawer"
	data-note-metadata-drawer
	aria-hidden="true"
	aria-labelledby="note-metadata-heading"
	tabindex="-1"
>
	<div class="note-metadata-drawer__shell">
		<header class="note-metadata-drawer__header">
			<div class="min-w-0">
				<p class="text-[10px] font-semibold uppercase tracking-[0.18em] text-[#728078]">
					{ui.noteDetail.metadata}
				</p>
				<h2 id="note-metadata-heading" class="mt-1 truncate font-serif text-xl font-semibold tracking-[-0.025em] text-[#1d2926]">
					{note.title}
				</h2>
			</div>
			<button
				type="button"
				class="inline-flex h-11 w-11 shrink-0 items-center justify-center rounded-full border-0 bg-transparent text-[#667277] transition hover:bg-[#eef1ee] hover:text-[#1f2b2e] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#64816e]"
				data-note-metadata-close
				aria-label={ui.noteDetail.closeMetadata}
			>
				<Icon name="x" class="h-5 w-5" />
			</button>
		</header>

		<div class="note-metadata-drawer__content" data-note-metadata-scroll>
			{
				note.imageUrl && (
					<div class="overflow-hidden rounded-lg bg-[#eef1ee]">
						<img
							src={note.imageUrl}
							alt=""
							class="max-h-[26rem] w-full object-cover"
						/>
					</div>
				)
			}
			<NoteProperties note={note} />
		</div>
	</div>
</aside>
