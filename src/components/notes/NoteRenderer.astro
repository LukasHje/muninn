---
import type { LibraryItem } from "lib/vault";
import { getNoteLayout } from "lib/noteLayout";
import CoverNoteLayout from "components/notes/CoverNoteLayout.astro";
import PlainNoteLayout from "components/notes/PlainNoteLayout.astro";
import TravelNoteLayout from "components/notes/TravelNoteLayout.astro";

interface Props {
	note: LibraryItem;
}

const { note } = Astro.props;
const layout = getNoteLayout(note);
---

{
	layout === "travel" ? (
		<TravelNoteLayout note={note} />
	) : layout === "cover" ? (
		<CoverNoteLayout note={note} />
	) : (
		<PlainNoteLayout note={note} />
	)
}
