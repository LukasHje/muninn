---
import { ui, withSiteName } from "src/i18n";
import VaultNotesBrowser from "src/components/VaultNotesBrowser.astro";
import MainLayout from "src/layouts/MainLayout.astro";
import { getRecentVaultItemsWithinDay } from "src/lib/vault";

const recentItems = await getRecentVaultItemsWithinDay();
---

<MainLayout title={withSiteName(ui.pages.recent.title)}>
	<VaultNotesBrowser
		title={ui.pages.recent.title}
		items={recentItems}
		emptyMessage={ui.pages.recent.empty}
		basePath="/recent"
	/>
</MainLayout>
