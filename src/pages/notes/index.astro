---
import { ui, withSiteName } from "src/i18n";
import VaultNotesBrowser from "src/components/VaultNotesBrowser.astro";
import MainLayout from "src/layouts/MainLayout.astro";
import { getLibraryItems } from "src/lib/vault";

const libraryItems = await getLibraryItems();
---

<MainLayout title={withSiteName(ui.pages.allNotes.title)}>
	<VaultNotesBrowser
		title={ui.pages.allNotes.title}
		items={libraryItems}
		emptyMessage={ui.pages.allNotes.empty}
		basePath="/notes"
	/>
</MainLayout>
